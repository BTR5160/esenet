# ESENet 2025 Landing Page

A Vite + React single-page application for the ESENet 2025 "Synapse: AI ∞ Business" event hosted at Technopole Manouba.

## Getting started

```bash
npm install
npm run dev
```

The development server runs on port 5173 by default. Use `npm run build` to produce a production-ready bundle.

## Tech stack

- Vite
- React with TypeScript
- Tailwind CSS
- shadcn/ui

## Deployment

The project is configured for static hosting. After running `npm run build`, deploy the contents of the `dist/` directory to your preferred hosting provider.
